Build Instructions:

git clone https://github.com/grogg/build-scripts.git -b l AOSP && cd ~/AOSP

repo init -u https://github.com/grogg/platform.git -b l

repo sync

./build-"device".sh

** "device" may be either deb, flo, or hammerhead **
