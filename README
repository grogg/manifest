Build Instructions:

git clone https://github.com/grogg/build-scripts.git -b lollipop AOSP && cd ~/AOSP

repo init -u https://github.com/grogg/platform_manifest.git -b 6.0.0_r1

repo sync

./build-"device".sh

** "device" may be either deb, flo, or hammerhead **
